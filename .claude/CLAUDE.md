<!-- Claude Code project memory.
     The shared brief is ../AGENTS.md at the repo root — the single, cross-tool
     source of truth. We import it here so Claude Code loads exactly what every
     other AI tool reads, with nothing to keep in sync.
     Edit AGENTS.md, not this file. -->

@../AGENTS.md
